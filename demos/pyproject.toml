[project]
name = "langfuse-deep-workshop-demos"
version = "0.1.0"
description = "Runnable demos paired with the Langfuse Deep Workshop (Level 300)."
requires-python = ">=3.11"
dependencies = [
    "langfuse==4.5.1",
    "openai>=2.33.0",
    "python-dotenv>=1.0.0",
    "requests>=2.33.1",
]
